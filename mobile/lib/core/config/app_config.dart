import 'package:flutter/material.dart';

class AppConfig {
  static const String appName = '会员管理系统';
  static const Color primaryColor = Color(0xFF165DFF);
  static const String apiBaseUrl = 'http://localhost:8080/api/v1';
  
  // 断点阈值（用于响应式布局）
  static const double phoneBreakpoint = 600;
  static const double padBreakpoint = 900;
}
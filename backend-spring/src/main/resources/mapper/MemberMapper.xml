<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.membercard.mapper.MemberMapper">

    <resultMap id="memberResultMap" type="com.membercard.entity.Member">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="birthday" column="birthday"/>
        <result property="tagsJson" column="tags_json"/>
        <result property="sourceChannel" column="source_channel"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findPagedByTenantId" resultMap="memberResultMap">
        SELECT * FROM member
        WHERE tenant_id = #{tenantId}
        ORDER BY created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countByTenantId" resultType="int">
        SELECT COUNT(*) FROM member WHERE tenant_id = #{tenantId}
    </select>

    <select id="searchByKeyword" resultMap="memberResultMap">
        SELECT * FROM member
        WHERE tenant_id = #{tenantId}
        AND (name LIKE CONCAT('%', #{keyword}, '%') OR phone LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countByKeyword" resultType="int">
        SELECT COUNT(*) FROM member
        WHERE tenant_id = #{tenantId}
        AND (name LIKE CONCAT('%', #{keyword}, '%') OR phone LIKE CONCAT('%', #{keyword}, '%'))
    </select>

</mapper>
